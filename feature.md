# Cornchip Day — Feature Specification

## MVP Vertical Slice Features

### F1 — Player Movement
- Run left/right via keyboard (arrow keys and/or A/D)
- Single jump (Space)
- No double-jump or other upgrades in the MVP — those arrive later as unlockable, level-gating abilities (see Phase 2 in `plan.md`)

### F2 — Obstacle Interaction & Lives
- One obstacle type for the MVP: thrown "mild" salsa (per `game-brief.txt`)
- On contact: a comedic visual reaction (e.g. Cornchip wilts or slips), then a brief respawn at the last safe point -- this per-hit feel is unchanged
- Every hit also costs one of 3 lives, shown as icon-only HUD (no numbers/text) in the top-left. At 0 lives, the whole level restarts from the beginning with lives reset to 3 -- no game-over screen. `level_base.gd` owns the life count; `player.gd` just emits `player_hit` on every hit regardless of source, so any future hazard gets lives-tracking for free.

### F3 — Ingredient Collection, Level Completion & Level Transition
- The level's ingredient is no longer just sitting out in the open — it spawns at the boss's position once the boss is defeated (see F5)
- Picking it up triggers a clear, purely visual celebration — no text
- Collecting it now also transitions to the next level (`level_base.gd`'s `next_level_path`, set per-level as an exported property): a boss's `_die()` calls `register_level_ingredient()` on the level root when it spawns the ingredient (since the ingredient doesn't exist yet at boss-defeat time, and the boss itself may already be freed by the time the player actually walks over and collects it seconds later)
- `level1.gd` was generalized into `level_base.gd` so every level reuses one script (same pattern as `boss.gd`), configured per-level via exported properties rather than needing a new script each time

### F5 — Stomp-to-Defeat Combat & Boss Battles
- Cornchip's baseline way to interact with enemies: jumping on them from above defeats them (detected via the player's fall velocity and position relative to the enemy at contact, not precise collision-normal math)
- Touching an enemy any other way (from the side, while not falling onto it) triggers the same stun-and-respawn as any obstacle, via the existing `hit_by_obstacle()`
- Implemented as a reusable `boss.gd` (patrol between two points, periodic projectile attack, N-hit health, defeat animation) rather than one script per boss, since more bosses are already planned
- The vertical slice's level now ends with a real boss fight: Hot Sauce patrols, fires a horizontal projectile at intervals, and takes 3 stomps to defeat; defeating him spawns the level's ingredient
- Promoted out of the backlog (was FB8/FB9) once actually built — see `plan.md` for status

### F6 — Little Enemies
- Cheese and Salsa Bowl both appear as regular in-level enemies (resolved from the boss-vs-enemy open questions in `characters.txt`): same `boss.gd` script as bosses, but Cheese is configured with 1 hit point/no projectile/tighter patrol, and Salsa Bowl with 2 hit points and a projectile (reusing `BossProjectile.tscn`) for a slightly tougher, ranged encounter
- Defeated the same way as any boss (stomp from above); a side touch or getting hit by Salsa Bowl's projectile costs a life like any obstacle
- Cheese's "puts Cornchip to sleep" effect (per the original brief, tuned down from the original 10s to 3s after testing) is now built: `boss.gd`'s `sleep_duration` export, and `player.gd`'s `put_to_sleep()` (input disabled, no life lost -- distinct from the generic hit stun). Cheese is configured with `sleep_duration = 3.0`; every other enemy/boss leaves it at the default 0 (normal hit stun).

### F7 — Scattered Bean Tokens
- Multiple bean pickups placed throughout the level (`BeanToken.tscn`, reusing `ingredient.gd`'s generic pickup logic with new placeholder art, tagged with the `bean_token` group instead of ending the level)
- Corrected from an earlier "lettuce token" naming mixup (additions.txt) -- beans are the scattered collectible currency; lettuce stays an ingredient-only concept
- Separate from the boss-dropped level ingredient, which is unchanged
- Counted in `level_base.gd` (`bean_tokens_collected`); no on-screen counter yet -- flagged as a follow-up, not built this pass

### F8 — Air Fryer Power-Up (Spin Dash)
- A pickup (placeholder art) that grants Cornchip a spin-dash: pressing Up (`ui_up`, already bound by default -- avoided hand-editing the InputMap) triggers ~1 second where touching any enemy defeats it instead of hurting him, same code path as a stomp
- Visual feedback is a color tint on the sprite for now, not a real spin animation -- flagged as future art/polish, not a functional gap

### F9 — Level 1 Rework: Jump Gaps, Length, Visual Spectacle, Arena
Direct user request ("make Level 1 grander/more epic"), covering four things at once:
- **Jump gaps:** the single continuous ground slab is now 5 separate segments (`GroundA`-`GroundD` plus `GroundArena`) with 4 real gaps between them. Falling through one is handled by `player.gd`'s new `FALL_RESPAWN_Y` check in `_physics_process` -- crossing that Y threshold calls the existing `hit_by_obstacle()`, so a missed jump costs a life exactly like any other hit rather than needing a separate mechanic. Gaps are sized (~90-100px) to stay comfortably within the player's max jump distance (~154px at full run speed) for a 5-8 year old audience.
- **Longer level:** roughly doubled in length (now ~3900 units, was ~2000), with enemies and bean-token clusters spread across the extra space rather than just padding empty ground.
- **Visual spectacle:** a layered sunset-patio background (three-band gradient sky, repeating string-light clusters, cactus silhouettes) replacing the flat sky-blue `ColorRect`, all procedural vector art -- no new paid generation for this pass.
- **Arena:** a distinct zone for the Hot Sauce fight (its own ground color, festive bunting at the entrance) so reaching him reads as arriving somewhere, not just more of the same ground.

### F4 — First-Pass Art
- AI-generated Cornchip sprite: idle, run, jump, and "hit" reaction states
- AI-generated sprites for the salsa obstacle, lettuce ingredient, and a cheese ingredient icon (all previously placeholder, now real art)
- Simple background/tileset for the single level
- **Pipeline note:** sprites are generated on a solid magenta (`#FF00FF`) background rather than requested as "transparent" — diffusion image models can't natively output real alpha, so a transparency request just yields a checkerboard baked into opaque pixels; shadows are explicitly forbidden in the prompt too, since a soft shadow blends with the magenta rather than matching it and survives chroma-keying as a smudge. Each character is generated as a single 8-pose grid image (idle, idle variant, 2-frame move cycle, 2-frame signature action, hit reaction, celebrate/defeated) rather than separate stills, covering Cornchip plus the confirmed bosses ahead of their Phase 2 implementation. `tools/generate-sprites.ps1` produces the raw grid images; `tools/crop-sprites.ps1` keys out the magenta, isolates each of the 8 poses as its own blob, and normalizes them onto a shared per-character canvas (bottom-anchored) so frame-switching doesn't jitter. Non-character props stay single still images since "8 poses" doesn't apply to a static object.
- **Pipeline hardening (post-Level-1):** generation requests now attach existing character art (Cornchip, Hot Sauce) as reference images alongside the text prompt, for style consistency on new characters rather than relying on prompt wording alone. The grid-slicer clusters rows by detecting Y-gaps instead of assuming a fixed 2-row layout, since the model doesn't reliably honor the requested 4x2 arrangement (Queso Grande came back as 2x4). Transparent pixels are now zeroed on RGB as well as alpha -- previously alpha=0 was correct but the color channels still had magenta baked in underneath, harmless in Godot but sloppy. The crop script was also fixed after it briefly reprocessed and corrupted every already-finalized frame by running its "crop to largest blob" pass against files it had already produced; it now refuses to touch its own prior output.
- Queso Grande and Jalapeño (Level 2 boss/enemy) art generated and confirmed by the user -- clear to build into scenes.

### F10 — Level 2 (Nacho Kitchen)
- First level built from the confirmed Bestiary by Level plan. Boss: Queso Grande (3-hit, fires a cheese-glob that slows Cornchip instead of costing a life -- a new hazard type, see `scripts/slow_projectile.gd` and `player.gd`'s `apply_slow()`). Enemies: Jalapeño (new, 1-hit melee) and Cheese (returning, its sleep effect already live).
- Its own ingredient: `CheeseIngredient.tscn` (new, distinct sprite from Level 1's lettuce), dropped by Queso Grande on defeat, same as Hot Sauce's pattern.
- Ground has 2 real jump gaps, shorter overall than Level 1's "epic" length (that treatment was specific to Level 1 as the onboarding level).
- Nacho-yellow layered background (gradient sky, tortilla-chip and cheese-drip decorations), same procedural-art approach as Level 1.
- `next_level_path` points to `Level3.tscn`, which doesn't exist yet -- matches the same forward-declared pattern Level 1 used for Level 2 before it existed.

### F11 — Level 1/2 Environment Art Pass (Obstacles, Hazard Fills, Ground Tiles, Parallax Depth)
User-requested: levels should have jump-over obstacles (not just gaps to jump across) themed per level, gap-hazards should look like something thematic instead of a plain void (e.g. Level 1's gaps full of hot salsa), and a fuller environment art pass beyond just character sprites. Scoped to Levels 1-2 only for this pass (already built); Levels 3-7 get their own art when they're actually built. Status: **all 16 assets generated, cropped, and wired into `Level1.tscn`/`Level2.tscn`, including a working parallax background. Not yet playtested. Open item: some duplicate/nested nodes in Level1 (see `plan.md`) worth reviewing next session.**

**Wiring notes (current, post-fixes -- see `plan.md` for the multi-round history of getting here):** obstacles are `StaticBody2D` + `CollisionShape2D` + `Sprite2D`, sized (collision height 32/46/58px for small/medium/large) to stay comfortably under the player's ~73px jump apex (`JUMP_VELOCITY`/`GRAVITY` in `player.gd`). Ground-tile and hazard-fill `TextureRect`s need `expand_mode = 1` (`IGNORE_SIZE`) + `stretch_mode = 6` (`KEEP_ASPECT_COVERED`) explicitly set -- `TextureRect` defaults to `expand_mode = KEEP_SIZE`, which ignores anchors/offsets entirely and renders at native pixel size, and the default stretch mode warps a wide-aspect source into a short box. Cactus/string-lights (L1) and chip/cheese-drip (L2) scenery replaced the old procedural `Polygon2D` decorations, though `Sprite2D` centers on its own origin so the Y position needs adjusting from the old polygon's position to actually touch the ground line, not copied verbatim. The window-view background lives in Godot's native `ParallaxBackground`/`ParallaxLayer` (not a custom script -- see plan.md for why that detour happened), tiled edge-to-edge as the whole backdrop (the flat `SkyTop`/`SkyMid`/`SkyHorizon` bands were removed), with `motion_scale = Vector2(0.4, 1.0)` -- X gets the parallax depth reduction, Y stays at full 1:1 camera tracking so the background never detaches from the ground during a jump. A dedicated backdrop fill (also inside the `ParallaxBackground`, never as a plain scene-tree node -- that draws in front of it, not behind) covers any gray showing past the tiled art's edges/seams. The Hot Sauce arena floor was deliberately left as its distinct dark-red `ColorRect` rather than covered with the ground tile, to preserve the boss-arena visual distinction built in F9.

**Bug found while wiring:** `l1`/`l2_ground_tile.png` and `l1`/`l2_hazard_fill.png` were actually JPEG-encoded bytes saved with a `.png` extension -- Godot's texture importer rejected them ("Failed loading resource"), caught via a headless `--import` run. Re-encoded as real PNGs, cleared the stale `.import` metadata Godot had cached from the failed first attempt, and hardened `generate-sprites.ps1` to detect and auto-fix this (JPEG magic bytes) on any future generation.

**Generation outcome:** the first pass (default `gemini-3.1-flash-image` model) had real defects on 7 assets: readable text baked onto jar/label art despite the "no text" instruction, character faces (Hot Sauce/Cornchip) bleeding into pure textures and scenery because reference images were being sent even for non-character prompts, and `l1_string_lights` not matching its prompt at all (came back as one plain ball instead of 3 colored bulbs on a wire). Fixed by: (1) adding a `NoRefs` flag to `generate-sprites.ps1` so texture/scenery prompts skip the character reference images, and (2) regenerating the 7 affected assets (`l1_obstacle_small/medium/large`, `l1_ground_tile`, `l1_string_lights`, `l1_window_view`, `l2_ground_tile`) with `gemini-3-pro-image` instead, per user's choice. All 7 confirmed clean on re-inspection.

**Pipeline bug found and fixed while verifying the regen batch:** `crop-sprites.ps1`'s chroma-key removal is a border flood-fill, which misses magenta pockets fully enclosed by outline pixels (e.g. the gap where 3 stacked jars meet on `l1_obstacle_medium`) — left a stray magenta sliver in the crop. A blanket "remove small enclosed background-colored pockets" fix was tried and reverted: it also ate legitimate anti-aliased gradient art (`l1_window_view`'s sunset-purple hills fractured into speckled noise, since dithering creates many small same-hued islands that look identical to a real leftover sliver by size alone). The one confirmed leftover-sliver case was fixed manually per-asset instead; `crop-sprites.ps1` itself keeps the original safe border-flood-fill behavior, plus a new `-Only <filenames>` param so a partial regeneration can be re-cropped without touching already-finalized assets (re-running the crop on an already-cropped file is unsafe — its corner pixel is transparent, not magenta, breaking the color reference sample).

**Mechanics (need no new code):**
- Jump-over obstacles are a plain solid wall -- same collision pattern as the ground segments, just a raised block placed mid-ground. The player's existing movement/collision handles walking into or jumping over it with zero new script logic.
- Pit-hazard fill is purely visual -- a themed texture (e.g. bubbling salsa) drawn over/in the existing gaps. The fall consequence (`FALL_RESPAWN_Y` -> `hit_by_obstacle()`, see F9) is completely unchanged.
- The window/parallax background element is the one piece needing new code: implemented as a Godot `ParallaxLayer` (inside a `ParallaxBackground`) so it scrolls slower than the foreground gameplay layer, producing real depth rather than just an illustrated trick.

**Obstacle variants:** 2-3 height/size variants per level (user request), reusing the same themed design at different stack heights so a level doesn't just repeat one exact silhouette everywhere.

**Asset list (16 total), Level 1 -- Salsa Cantina:**
| File | Prompt |
|---|---|
| `l1_obstacle_small.png` | A cartoon obstacle for a platformer: a single stack of two red salsa jars with cartoon labels, glossy glass, a small comedic wobble to the stack. |
| `l1_obstacle_medium.png` | Same, but a small pyramid of three stacked salsa jars. |
| `l1_obstacle_large.png` | Same, but a tall pyramid of five stacked salsa jars, slightly leaning for a comedic wobble. |
| `l1_hazard_fill.png` | Seamless tileable texture: bubbling hot molten salsa, glossy red-orange, bubble highlights, subtle simmering texture, no visible seam. |
| `l1_ground_tile.png` | Seamless tileable texture: warm terracotta patio floor tiles with grout lines, warm reddish-brown tones, no visible seam. |
| `l1_cactus.png` | A simple cartoon saguaro cactus silhouette, two side arms, bright green with highlight lines. |
| `l1_string_lights.png` | A short strand of 3 festive string lights (red, yellow, green bulbs) on a thin wire. |
| `l1_window_view.png` | A wooden cantina-style window frame set into a warm adobe wall, with a hazy, softer-detailed distant desert scene visible through the glass: silhouettes of far-off cacti and low hills under a warm sunset sky, suggesting depth beyond the window. |

**Asset list, Level 2 -- Nacho Kitchen:**
| File | Prompt |
|---|---|
| `l2_obstacle_small.png` | A single nacho serving tray standing on its edge, red plastic basket-style tray with a few tortilla chips visible. |
| `l2_obstacle_medium.png` | Two stacked nacho serving trays, same style. |
| `l2_obstacle_large.png` | Three stacked nacho trays topped with a block of yellow cheese. |
| `l2_hazard_fill.png` | Seamless tileable texture: bubbling molten nacho cheese, glossy orange-yellow, bubble highlights, melty texture, no visible seam. |
| `l2_ground_tile.png` | Seamless tileable checkered nacho-bar floor, alternating warm yellow and tan tiles, no visible seam. |
| `l2_chip.png` | A single cartoon tortilla chip, triangular, golden-tan with brown speckles. |
| `l2_cheese_drip.png` | A single cartoon cheese-drip/dollop hanging shape, glossy orange-yellow. |
| `l2_window_view.png` | A window frame set into a tiled cheese-yellow kitchen wall, with a hazy, softer-detailed distant scene visible through the glass: a softly-lit dining area or second kitchen counter, suggesting depth beyond the window. |

**Generation notes:** obstacles and background-scenery items (cactus, string lights, chip, cheese drip, window views) are icon-style assets on solid magenta, same treatment as character art. Hazard-fill and ground-tile textures are NOT magenta/chroma-key assets -- they're requested directly as seamless tileable patterns, since the whole image is the visible texture rather than an icon to key out. Same reference images (`cornchip_frame1.png`, `hot_sauce_boss_frame1.png`) used for all, for style consistency.

### F12 — Level 3 (Guac Stand)
- Boss: Avocado (3-hit, jump-stomp -- confirmed by the user as the standard defeat condition, same as every other boss so far). Attack is a new hazard *type*, not a projectile: `boss.gd` gained a `hazard_scene` export (parallel to `projectile_scene`) that drops a static ground hazard at the boss's own feet on the attack beat instead of firing something at the player. `GuacPuddle.tscn`/`guac_puddle.gd` stuns on touch like any obstacle and self-frees after 4 seconds so puddles from a long fight don't keep piling up in the arena.
- Enemies: Lime (new, 2-hit ranged -- reuses the plain `BossProjectile.tscn`, same "standard hit" tier as Salsa Bowl) and Onion (new, 1-hit, stationary via `boss.gd`'s `move_speed = 0`). Onion introduces a genuinely new interaction: proximity, not touch, matters. `boss.gd` gained a `wobble_radius` export, checked every physics frame against the player's distance (not an Area2D touch event); in range, it calls a new `player.gd` method `apply_screen_wobble()`, which jitters `Camera2D.offset` with a sine wave -- the same technique already used for the run-cycle sprite bob, just applied to the camera instead. No life cost, matching the "a nuisance, not a hit" spec in `characters.txt`.
- Lime's sprite sheet came back with 9 poses instead of the standard 8 (an extra squirt-attack frame, same anomaly plan.md already noted for this batch) -- used frames 5 and 6 as the two attack phases and skipped 7, per the standing "pick 2 of the 3, no cleanup needed" call.
- Its own ingredient: `GuacIngredient.tscn` is a **procedural placeholder** (a `Polygon2D` blob, no generated art yet) -- Level 3's art pass (obstacles, ground tile, hazard fill, parallax background, ingredient icon, equivalent to F11 for Levels 1-2) hasn't happened yet and needs its own confirmed prompt batch before any paid generation runs, per standing practice. Jump-over obstacles are likewise plain procedural `Polygon2D` crates for the same reason -- both are placeholders, not a finished art pass.
- Ground has 2 real jump gaps, same overall scale as Level 2 (this level doesn't get Level 1's "epic" treatment either).
- `next_level_path` points to `Level4.tscn`, which doesn't exist yet -- same forward-declared pattern as every prior level.

### F13 — Level 4 (Market Tomatoes)
- Boss: Big Red, a genuinely new fight structure -- an "evolving-fight-pattern boss" per `characters.txt`, confirmed with the user before building since nothing else in the game changes mid-fight like this. `boss.gd` gained three exports for it: `split_into_scene`/`split_count`/`split_offset_x` (on the killing blow, spawn N copies of another scene at itself instead of the normal death) and `shared_defeat_group` (a piece only drops the ingredient on death if no other member of a named group is still alive). Both spawned Cherry Tomato pieces inherit Big Red's own `ingredient_scene`/`ingredient_spawn_position` and a group name unique to that split event (`"split_%d" % get_instance_id()`), generated at spawn time rather than hand-authored in a scene file.
- **Real bug caught by headless verification before shipping:** the first version of `shared_defeat_group`'s check (`get_nodes_in_group(...).size() > 1`) was racy -- `queue_free()` doesn't remove a node from its groups until the deferred free actually runs a moment later, so if both Cherry Tomato pieces are stomped in the same frame, each one's death-check still sees the other as "still alive" (neither had left the group yet) and *both* skip the ingredient drop, softlocking the level. Fixed by having each piece call `remove_from_group()` immediately/synchronously as the first step of its own death, before checking whether anyone else remains -- confirmed via a headless script that kills both pieces back-to-back and asserts exactly one ingredient spawns (was 0 before the fix, 1 after).
- Enemies: Cherry Tomato (new, 1-hit, fast, melee-only -- no attack animation wired, same pattern as Cheese/Jalapeno since it never fires anything) appears both standalone and as Big Red's split result. Salsa Bowl returns with a lower `fire_interval` (2.5 vs. the default 4.0) for a faster-firing "tougher" version, entirely via instance override -- no script changes needed.
- Cherry Tomato's art needed two generation passes: the default model baked visible text labels into every pose (violates the no-reading-required pillar outright), fixed by regenerating with `gemini-3-pro-image`, the same fix that worked for the L1/L2 text-defect batch in F11.
- Its own ingredient: `TomatoIngredient.tscn`, another procedural placeholder like Level 3's -- Level 4 environment art is deferred the same way, not yet scheduled.
- `next_level_path` points to `Level5.tscn`, not yet built.

### F14 — Level 5 (Frosty Fridge)
- Boss: Sour Cream Sam is the first boss with no attack of any kind (`projectile_scene`/`hazard_scene`/`split_into_scene` all unset) -- per `characters.txt`, the icy arena floor itself is the entire challenge, "changing how Cornchip's movement feels rather than adding another attack type." New `IceZone.tscn`/`ice_zone.gd`: a plain `Area2D` region trigger (not a hazard -- no life cost) that calls a new `player.gd` method `set_on_ice()` on entry/exit. While `is_on_ice` is true, horizontal movement switches from the normal instant velocity snap to `move_toward(velocity.x, target, ICE_ACCEL * delta)`, so input lags into a slide instead of stopping/turning immediately. Confirmed with the user before building since it's a new player-movement mechanic, not just an enemy config. Deliberately built as a generic reusable zone (not Sour-Cream-specific) for Level 6's heating floor to reuse the same pattern later.
- Enemies: Ice Cube (new, 1-hit, the fastest `move_speed` of any character yet, matching its "fast" spec) and Chive Bit (new, 1-hit, the smallest `Sprite2D` scale of any character yet, matching "tiny" -- placed as 3 instances clustered together for "small swarms," no new spawner code needed).
- Verification note: the `is_on_ice` zone-detection/toggle was confirmed correct via a headless script (teleporting the player in and out of the zone and checking the flag). The `move_toward` velocity-ramp itself couldn't be verified the same way -- simulated `Input.action_press()` didn't register through this particular headless harness, a test-tooling limitation rather than a sign of an engine or code issue (the toggle logic driving it is proven correct, and `move_toward` is a standard, well-understood built-in). Left for the user to feel-check directly during play, same as any other movement-feel question.
- Its own ingredient: `SourCreamIngredient.tscn`, another procedural placeholder -- Level 5 environment art deferred the same way as Levels 3 and 4.
- `next_level_path` points to `Level6.tscn`, not yet built.

### F15 — Level 6 (Sizzling Griddle)
- Boss: Iron Skillet is the second boss with no attack at all (`projectile_scene`/`hazard_scene`/`split_into_scene` unset), same as Sour Cream Sam -- the arena floor is the whole challenge. New `HeatZone.tscn`/`heat_zone.gd`, the sibling of Level 5's `IceZone`: a self-looping timer cycles safe (4s) -> warning flash (0.8s, a fairness cue for the target 5-8 year old audience) -> hot (2s) -> repeat. While hot, any body inside the zone that's grounded (`is_on_floor()`) and has a `hit_by_obstacle` method takes exactly one hit per hot phase (a per-cycle latch prevents repeat hits even if the player recovers and stays put). Confirmed with the user before building. **Verified via a headless script** driving a real `Player.tscn` instance settled on a floor inside a fast-cycling zone: hit fires exactly once at the hot-phase transition, latch blocks a second hit through the rest of the phase, resets correctly on the next cycle.
- Enemies: Jalapeño and Onion return tougher via plain instance overrides in `Level6.tscn` (Jalapeño: `max_health` 1->2, `move_speed` 55->65; Onion: `wobble_radius` 100->140) -- no script changes. Grease Splatter (new) is the first enemy that's genuinely immune to stomping: `boss.gd` gained a `cannot_be_stomped` export that forces `is_stomp` false regardless of fall angle, so any contact stuns the player like a side-touch instead of defeating it. The Air Fryer spin-dash still works on it (no stated exception to its "defeats anything you touch" rule in F8). **Verified via headless script**: a stomp-shaped contact leaves it undamaged and stuns the player; a spin-dash contact still defeats it.
- **Real fairness bug found via playtest, fixed in two rounds:** Grease Splatter originally patrolled, which the user reported as literally impossible to clear. Making it stationary (first fix) helped but wasn't enough -- reworking the jump-arc math showed the stationary hazard's own height still made it geometrically uncrossable by a single jump (the "high enough" window covers less horizontal distance than the hazard+player width requires). Real fix: **F16, double jump**, see below.

### F16 — Double Jump (Level 6+)
Added directly in response to the Grease Splatter fairness bug, per the user's own suggested fix. `player.gd` gained `has_double_jump`/`air_jumps_used`: one extra jump usable while airborne, reset whenever `is_on_floor()` becomes true (so it works whether the first ascent was a jump or just walking off a ledge). Deliberately gated to start at Level 6 rather than granted from the beginning -- Levels 1-5's jump gaps were tuned around the original single-jump ~154px range, and a global double jump would trivialize them. Granted via a new `level_base.gd` export, `grants_double_jump`, set per-level (`Level6.tscn` has it; `Level7.tscn` will need it too when built) rather than trying to persist ability state across the scene-reload boundary between levels -- the same lesson already learned from Air Fryer/spin-dash not surviving `change_scene_to_file()`. No new art needed; the existing "jump" animation already covers any airborne state.
Verified analytically rather than via a live headless playtest (simulated keyboard input doesn't register through this project's headless test harness -- same gap noted for Level 5's ice feel): a double jump timed near the first jump's apex reaches double the single-jump height and covers roughly 205px of "high enough" horizontal distance against Grease Splatter's 100px danger zone -- comfortable, over 2x margin.
- Its own ingredient: `CrunchyShellIngredient.tscn`, another procedural placeholder -- Level 6 environment art deferred the same way as Levels 3-5.
- `next_level_path` points to `Level7.tscn` (the Wrap finale), not yet built. This closes out Phase 2's Levels 2-6 -- only Level 7 remains before the full 7-level MVP is complete.

### F17 — Level 7 (The Wrap Finale)
- Tone resolved by direct user design, not the open question it was in earlier docs: no combat at all for the finale. Structure: a remix gauntlet (one returning enemy per Level 1-6 -- Cheese, Jalapeño, Lime, Cherry Tomato, Ice Cube, Grease Splatter -- no new assets) leads to Wrap's arena.
- Wrap himself is deliberately **not** a `boss.gd` instance -- new `wrap.gd`/`WrapBoss.tscn`. He has no health, doesn't patrol, isn't damaged or defeated; forcing that mismatch into the shared boss script would have stretched it well past its actual model (a combat entity) for an object that's fundamentally a narrative set-piece. He tracks deliveries via any node in a new `wrap_delivery` group (same idiom `level_base.gd` already uses for `bean_token`) rather than needing bespoke wiring per delivery item.
- The 6 "delivery" ingredients are the exact same `Ingredient`/`CheeseIngredient`/`GuacIngredient`/`TomatoIngredient`/`SourCreamIngredient`/`CrunchyShellIngredient` scenes already built for Levels 1-6 -- just placed as pickups in this level, no new scenes needed. **No new cross-level save state was needed either**: since the level chain only advances by collecting each level's ingredient, reaching Level 7 at all already proves all 6 were earned along the way.
- Win condition: at 6/6 deliveries, Wrap switches to his existing frame-8 pose (generated back in Phase 1 as "a warmer, softer smiling pose" -- a perfect fit found rather than needing new art) and a jump-onto landing (the exact same fall-and-land shape every boss already uses for a stomp) triggers `player.gd`'s new `win_celebration()`: physics processing stops, Cornchip plays a new `celebrate` animation (`cornchip_frame8.png`, generated in Phase 1 and never wired into `Player.tscn` until now). Per the user's explicit choice, no dedicated ending scene -- a simple in-place celebration, matching MVP scope.
- **Verified via headless script** end to end: 5/6 deliveries correctly withholds the light-up; a touch before lighting up does nothing; a non-jump-shaped touch even after lighting up does nothing; only a proper jump-onto landing after 6/6 triggers the win, freezing the player and playing the celebrate animation.
- Double jump (F16) is carried forward (`grants_double_jump = true` on `Level7.tscn`) since it's meant to be a permanent ability from Level 6 on, not lost at the finale.
- **This completes the full 7-level MVP** -- every level, boss, and mechanic in the confirmed plan is now built. Remaining work is polish: the Levels 3-6 environment-art pass (still procedural placeholders) and a full interactive playtest start to finish.

### F18 — Unique Boss Defeat Conditions
Direct user feedback after the full 7-level MVP was playable: "3 jumps on them all is a bit lame." Every boss's kill condition is now genuinely different, each reusing a mechanic that level already has rather than inventing something unrelated. All confirmed with the user before building (a full redesign of every boss fight is real new-content scope), and all 6 verified via isolated headless tests before wiring into the real scenes (one test caught a real bug -- see below).

`boss.gd` gained 5 new orthogonal exported flags, each defaulting off so existing bosses/enemies are unaffected unless explicitly opted in:
- **Hot Sauce / Avocado -- `vulnerable_only_during_attack`:** a stomp only lands during (and for `attack_vulnerability_window` seconds after) the attack beat that already exists for both of them (Hot Sauce's projectile fire, Avocado's guac-puddle drop). Mistimed, it's a harmless bounce (`velocity.y = -380`, same upward pop as a real hit) rather than a no-op or a punish -- you have to watch for the tell and time the jump, but guessing wrong costs nothing.
- **Queso Grande -- `spin_dash_only`:** a plain stomp bounces off harmlessly; only the Air Fryer spin-dash actually defeats him ("frying the cheese pot"). Since spin-dash doesn't persist across the scene-reload between levels (the same lesson double jump already ran into), `Level2.tscn` needed its own `AirFryer` pickup placed before his arena -- Level 1's doesn't carry over.
- **Sour Cream Sam -- `defeated_by_slide_charge`:** not stomped at all -- colliding into him at `slide_charge_min_speed` (150) or faster while sliding on his own ice defeats him; anything slower just hurts you like a normal touch. Turns the level's existing ice-sliding mechanic into an offensive tool, not just a hazard to endure.
- **Iron Skillet -- `heat_zone_path`:** a `NodePath` to the level's `HeatZone` (read once in `_ready()`), checked on every stomp attempt. Stomping while `heat_zone.is_hot` is true burns the player instead of hurting him -- only the cool phase is a safe opening. This is the one boss where a mistimed attempt has a real cost, not just a bounce, which feels right for the last "arena changes the fight" boss in the escalation (Sam -> Skillet).
- **Big Red** — unchanged; the split-into-two-Cherry-Tomatoes mechanic (F13) was already unique, no rework needed.

**Real bug caught during verification, not after:** the first test run showed Sour Cream Sam's slow-contact case incorrectly damaging him, and Iron Skillet's cool-phase case incorrectly doing nothing. Both turned out to be test-script bugs, not `boss.gd` bugs -- the first because none of the 4 test boss instances had been given separated positions (all defaulted to the same origin and their `HurtArea`s physically overlapped, so a leftover `Player` instance from an *earlier* section was still falling under real gravity and triggering *later* sections' bosses via the real Area2D signal), the second because `hit_by_obstacle()` zeroes the player's velocity as a side effect, so a second stomp attempt reused in the same test object was no longer stomp-shaped. Fixed the test (separated origins per boss, freed each player after its section, re-set velocity before reuse) rather than the code, and reran clean. Flagged here because it's a good example of why isolating variables matters even in throwaway test scripts, not just production code.

### F19 — Extra-Life Pickups and Jump-Challenge Bonuses
Both from the same round of direct user feedback as F18.
- **`LifePickup.tscn`**: reuses `ingredient.gd` verbatim (same touch-to-collect Area2D pattern as beans/ingredients, no new script) with `cornchip_frame1.png` -- the exact texture the HUD already uses for life icons, so a pickup in the world visually reads as "a life" without any new art generation. `level_base.gd` listens for a new `life_pickup` group (same idiom as `bean_token`/`wrap_delivery`) and `_on_life_pickup_collected()` increments `lives`, capped at `MAX_LIVES` (confirmed decision -- no HUD change needed, no 4th icon slot). Verified via headless script: heals a lost life and re-reveals the correct icon, caps cleanly at 3 rather than overflowing.
- **Bonus platforms**: one per level (all 7), each holding a `LifePickup`. Went through two real design corrections before landing on the final shape, both caught by verification rather than shipped blind:
  1. First attempt was a thin floating platform positioned by height above standing alone. Wrong: didn't account for the *player's own collision height* -- at the platform height needed for jump-reachability, the platform's underside already overlapped the player's head just standing normally underneath it, permanently wedging the player in place. Confirmed via two isolated headless repros (a bare `CharacterBody2D` and `Player.tscn` alone) that jump physics themselves are completely sound -- the bug was placement geometry, not the jump code.
  2. Reworked the math properly and found the valid height window for a floating platform of any reasonable thickness is only a few pixels wide, given this character's specific body-height-to-jump-height ratio -- technically satisfiable but too tight to trust without being able to empirically confirm it (see below).
  3. Final design: **not a floating platform at all** -- a solid pedestal running all the way from the ground up to landing height (60px, safely under the ~73.5px jump max), the same shape as the game's existing jump-over obstacles (already proven reachable via extensive real playtesting all session) just taller and flat-topped for landing on instead of clearing over. Eliminates the walk-under-clearance problem entirely since there's no gap to walk under.
  - **Verification note:** attempting to confirm final reachability via a headless jump-arc simulation against the real, busy level scenes (dozens of other live enemies/bosses also physics-processing) produced inconsistent results even after the placement fix, while two *isolated* minimal repros (empty scene, just one floor and one body) both showed a clean, correct, full-height jump arc with the same constants. This point is flagged as a test-harness limitation, not a code bug -- the underlying physics are proven sound in isolation, and the final pedestal height is well under the proven maximum with real margin, matching an already-extensively-playtested obstacle height. Worth an actual playtest confirmation like the other feel-only items (Level 5 ice, Level 6 heat).

### F20 — More Animation Variety for Cornchip and Wrap
Direct user request ("more textures for the main character and wrap"), fulfilled by wiring already-generated-but-never-used frames rather than paying for new art -- `cornchip_frame4.png` and `wrap_frame3.png`/`frame4.png` existed on disk since Phase 1 but were never added to either character's `SpriteFrames`.
- Cornchip's `run` animation was a single held frame with the actual sense of motion coming entirely from a procedural bob+tilt (`player.gd`'s `RUN_BOB_AMPLITUDE`/`RUN_TILT_DEGREES`, still there, now layered on top of a real 2-frame leg-alternating cycle instead of a static pose).
- Wrap's `idle` animation went from a 2-frame (blink/weight-shift) loop to a 4-frame loop adding his two "walking, arms crossed" poses -- he doesn't actually move in Level 7 (stationary the whole finale, per `wrap.gd`), so this is idle-only variety rather than a real walk cycle, but it gives him more visible life during however long the player takes to gather the 6 deliveries.

### F21 — Bonus Platform Relocation (Phase 4, Group 1)
Direct user feedback: all 7 levels' `BonusPlatform1`/`BonusLifePickup1` (see F19) sat at the same template position (x=935 in Level 1, x=945 in Levels 2-7) -- right after the first hazard gap, before any real stakes existed in the route. Moved deeper into each level, same proven solid-pedestal design (unchanged), only position changed:
- **Level 1** (935 -> 2730): past 3 of 4 hazard gaps and both Salsa Bowl enemies, shortly before the final gap into the Hot Sauce arena. Closest neighbor (BeanToken14) 70px clear.
- **Level 2** (945 -> 1650): past the Jalapeño/Cheese enemies and both mid-level jump obstacles, between the Air Fryer pickup and the Queso Grande arena. Closest neighbor (AirFryer) 90px clear.
- **Level 3** (945 -> 1800): past both Lime enemies and the first Onion, shortly before the Avocado arena. Closest neighbor (JumpObstacleLarge1) 100px clear.
- **Level 4** (945 -> 1800): past both Cherry Tomato enemies and the Salsa Bowl, shortly before the Big Red arena. Closest neighbor (JumpObstacleLarge1) 100px clear.
- **Level 5** (945 -> 1880): inside the ice-zone half of the level (unavoidable -- the ice floor covers the entire post-gap-2 route by design, and existing bean tokens already sit in that zone too), past the Ice Cube/Chive Bit cluster, before the Sour Cream Sam arena. Closest neighbor (boss) 170px clear.
- **Level 6** (945 -> 1880): same reasoning as Level 5 but inside the heat-zone half -- reaching it now requires timing the heat cycle (see `heat_zone.gd`), consistent with the level's own central mechanic. Closest neighbor (boss) 170px clear.
- **Level 7** (945 -> 1750): past the Grease Splatter enemy (unstompable), shortly before the delivery-item cluster and Wrap's arena. Closest neighbor (LettuceDelivery) 100px clear.

Verified via a temporary headless script (instantiate each level, measure `BonusPlatform1`'s x-distance to every other gameplay node, flag anything under 50px) -- all 7 levels clear with 70-170px margin to the nearest enemy/obstacle/delivery item; ground/decorative-sprite nodes excluded from the check since their `position` reflects the whole segment's anchor, not a collidable footprint. Script discarded after use, matching this project's established ad hoc verification pattern (not a persisted test).

Levels 5 and 6 placements deliberately land inside their level's ice/heat zone rather than avoiding it -- there was no viable pre-zone window large enough (the gap between the last enemy and the zone boundary was under 50px in both cases), and it's thematically consistent with "more difficult to find and use" per the raw feedback. Flagged here since it compounds with two already-open, not-yet-interactively-verified feel questions (ice-sliding precision, heat-cycle fairness) -- worth confirming during the eventual full playtest that landing on the pedestal while sliding/mid-heat-cycle feels fair, not cheap.

### F22 — Two-Tier Maze Paths, Levels 3-6 (Phase 4, Group 2)
Confirmed scope: a raised platform route running above the ground route for most of each level's length, both real options, rejoining before the boss arena. Same template geometry applied identically to L3-6 (they share identical ground/gap/obstacle x-positions), only enemy roster differs by level.

**Ascent design** (why it's a 2-jump staircase, not a single hop): the project's own F19 finding ruled out a thin floating platform at single-jump height (~60-70px) -- at that height a standing player's head already clips its underside (max jump height is exactly 73.5px; player collision is 50px tall; ground surface confirmed at world y=360 by cross-referencing `GroundA`'s collision offset against the proven bonus-pedestal geometry). So reaching the tier uses two chained single-jumps, each an independent ~60px rise (matching the pedestal's already-proven-safe height, not pushing toward the 73.5 ceiling):
- `UpperAccess1` -- solid ground-anchored block, ground to +60px (identical height to the F19 bonus pedestal), placed in the one genuinely clear stretch of each level's opening (x=115, well before `JumpObstacleSmall1` at x=300 -- an earlier attempt at x=260 left only 7px clearance from that obstacle and was moved after headless verification caught it).
- `UpperTier1-4` -- four thin (20px) floating platforms from x=235 to x=1605, top surface at +120px above ground (underside clearance ~100px above ground, well clear of the F19 danger zone). Internal gaps are 100-110px, same-height jumps well under the proven 154px max horizontal distance.
- Tier ends at x=1605, comfortably clear of the relocated bonus platforms (F21) at x=1800 (L3/4) and x=1880 (L5/6) -- 195-275px margin -- so the descent naturally rejoins the ground route right around where the bonus pedestal sits.

Verified via two temporary headless scripts (both discarded after use, per this project's established ad hoc pattern): one solving the actual jump-arc quadratic (using the real `player.gd` constants -- `JUMP_VELOCITY=-420`, `GRAVITY=1200`, `SPEED=220`, confirming the documented 73.5px/154px figures exactly) to confirm every gap and the ascent's landing window is reachable with real margin; a second instantiating all 4 modified level scenes headlessly to confirm they load cleanly with every new node present. Geometry is confirmed correct; as with every other jump-feel item in this project (ice sliding, heat cycling, the F19 pedestal itself), actual in-game *feel* still needs a real playtest -- headless math proves reachability, not that it feels good.

**L5/L6 note:** the tier's x-range overlaps the IceZone/HeatZone's x-range, but the tier sits well above their effective ground-contact interaction (players are only affected while grounded within the zone, and the tier is a separate floor); no code changes made to `ice_zone.gd`/`heat_zone.gd` for this — flagging as an assumption to confirm during playtest rather than scope creep into modifying the shared hazard-zone scenes.

### F23 — Wrap Delivery Ritual Rework (Phase 4, Group 3a)
Replaces F17's walk-over auto-collect with the confirmed carry-one-at-a-time ritual.

- **`ingredient.gd`** gained a `carry_to_wrap` export (default false, so every other use of this shared script -- beans, life pickups, every level's boss-dropped ingredient -- is untouched). Set true only on Level7.tscn's 6 `wrap_delivery` instances. On touch, instead of instantly collecting, it calls the new `player.gd` `start_carrying(self)` and disables its own monitoring; a new `deliver()` method (called by Wrap, not on touch) does the actual collect-and-fade.
- **`player.gd`** gained `carried_item` + `start_carrying()`/`take_carried_item()` -- a plain reparent to be a child of the player at a fixed `CARRY_OFFSET` above Cornchip's head, refusing a second item while one's already held (the "one at a time" from the raw feedback). Deliberately doesn't touch movement, jump, or stomp code at all, so carrying is safe by construction rather than by a special-cased check.
- **`wrap.gd`** reworked: the old `_ready()`-time auto-wiring of every `wrap_delivery` node's `collected` signal is gone (deliveries no longer happen on touch). `_on_touch_area_body_entered` now branches three ways on the same jump-onto contact: carrying + not lit up -> deliver (increment count, bounce feedback, light up at 6); not carrying + lit up -> the original final win; anything else -> no-op. Wrap drifts (`patrol_half_width=150`, `move_speed=55`, horizontal only, matching every other enemy's patrol style) once `deliveries_received > 0`, stopping and re-centering the instant he lights up -- so the first delivery is a free "tutorial" rep and the remaining five are timed.
- Delivery items repositioned in `Level7.tscn` from a tight 1850-2250 cluster (leftover from the old walk-past-everything layout) to a 1680-2500 spread around Wrap's own 1900-2200 patrol range, so carrying one actually means covering real ground instead of a near-instant hop, while staying clear of the F21 bonus platform at x=1750.

Verified via a headless script that scripts the entire ritual end-to-end on the real `Level7.tscn` (no mocks): picks up and delivers all 6 in sequence confirming `deliveries_received` climbs 1-6 and `is_lit_up` flips exactly on the 6th, then confirms the final jump-onto sets `is_complete`. A second script confirmed Wrap's `position.x` actually changes over simulated frames once `deliveries_received > 0`. Both discarded after use, per this project's established ad hoc verification pattern. Feel (is the drift speed/timing fun rather than fiddly) still needs a real playtest, same caveat as every other motion-based mechanic in this project.

### F24 — Level 7 New Content (Phase 4, Group 3b)
A new segment inserted between the existing remix gauntlet (ends ~x=1635) and the delivery arena (now starts x=2900, shifted out from its old 1680 start to make room). Blends the three flavors confirmed for this item:

- **Environmental combo (ice -> guac, x=1680-2200):** `IcePatch` (reuses `ice_zone.gd` as-is, a small custom-sized instance rather than the shared `IceZone.tscn`, which is hardcoded to Level 5's full arena size) immediately followed by `GuacPatch` (new `guac_patch.gd`, mirrors `ice_zone.gd`'s enter/exit toggle pattern exactly). Guac uses a new `player.gd` `set_on_guac()` -- a plain toggled `speed_multiplier` rather than the existing timed `apply_slow()`, specifically so a slow patch that's walked through rather than hit-and-run can't wear off mid-crossing. Both patches got a light ColorRect tint (blue/green) since, unlike L5/L6 where the *entire* arena is the hazard, this is a hazard-sized-smaller-than-the-room, and an invisible boundary would be genuinely confusing to find.
- **Hidden detour (x=2260):** one small platform reusing the exact bonus-pedestal shape/height (`RectangleShape2D_bonus_platform`, already proven jump-safe by F19/F21) holding 2 extra `BeanToken`s -- the "extra beans tucked behind branching" flavor, scoped down from a full branch to a single findable side-jump.
- **Chase corridor (x=2300-2800, no combat):** new `rolling_hazard.gd` (an `Area2D` moving at 260px/s, faster than the player's 220 run speed) paired with `chase_trigger.gd` (activates and repositions the hazard to its start whenever the player enters the corridor -- deliberately not one-shot, so a respawn after getting caught still finds a real chase rather than a spent one). Getting caught is a normal hit via `hit_by_obstacle()` -- 1 life, same consequence as everything else in the game, no new fail state.
- **Ground extended:** `GroundC`'s collision/visual widened from spanning 1390-2727 to 1390-3820 to cover the new segment plus the relocated delivery arena.

Note: the existing F21 bonus platform (x=1750) now sits inside the ice patch's x-range -- an emergent interaction (grabbing it means dealing with icy footing on the approach), not a conflict; solid-body collision and the Area2D trigger are orthogonal systems, verified to coexist without error.

Verified via a headless script on the real `Level7.tscn`: confirms every new node loads, `is_on_ice`/`speed_multiplier` toggle correctly on patch entry/exit, the chase trigger activates the hazard and a hazard-player contact correctly costs a life through the normal `level_base.gd` path, `GroundC`'s collision span is exactly `[1390, 3820]`, and the full 6-item delivery ritual still completes correctly at the new positions. As with F22/F23, headless checks prove correctness, not feel -- the chase's pacing (260 vs 220, 100px head start) and the ice->guac transition still need a real playtest.

### F25 — Title Screen & World Map (Phase 4, Group 4)
The last Phase 4 group, and the biggest architectural change: the project's first autoload/singleton.

- **`scripts/game_progress.gd`** (autoload `GameProgress`): `highest_unlocked_level`/`last_played_level`, plus `unlock_level()`/`is_unlocked()`/`level_scene_path()` helpers. In-memory only, matching the confirmed scope ("survives scene changes", not a save file) -- resets on app restart by design, no disk persistence added.
- **`scenes/TitleScreen.tscn`** is now `project.godot`'s `run/main_scene` (was `Level1.tscn` directly). Shows "Corn Chip Day" -- the one text exemption from the no-reading pillar -- plus an idle-animated Cornchip and a bouncing triangle prompt icon; `ui_accept` moves on to the world map.
- **`scenes/WorldMap.tscn`** (`world_map.gd`): 7 nodes in an axis-aligned zigzag (each segment is purely horizontal or purely vertical -- see the note below on why), avatar slides between them on directional input. `_matches_direction()` computes the dominant axis from the actual node positions rather than a hardcoded per-segment direction, so the `.tscn` layout stays the single source of truth. Forward movement past the unlock frontier is refused outright (`GameProgress.is_unlocked()`); backward movement to any already-unlocked node is always allowed, and pressing accept on any node -- ahead or behind -- launches that level, which is what makes replaying an earlier level "for more beans" work with zero special-case code.
- **`level_base.gd`** reworked: `next_level_path` (a per-level string export) is gone, replaced by `level_number` (a plain int, used to call `GameProgress.unlock_level(level_number + 1)`). Every level now returns to `WorldMap.tscn` on ingredient collection instead of chaining straight into the next level scene -- this is the actual "in between each level" flow from the raw feedback, not just a menu you see once at boot.
- Level 7 is untouched beyond gaining a `level_number = 7` export for bookkeeping -- its win condition still goes through `wrap.gd`'s `win_celebration()` (freezes in place), not this ingredient-collection/unlock path, since there's no Level 8 to unlock and no ending scene was ever in scope here.

**A genuine headless-testing gotcha, worth recording:** verifying an autoload requires actually loading a scene through the normal boot path (`godot --headless some_scene.tscn`) -- running a bare `SceneTree`-extending script via `godot --headless --script foo.gd` (this project's usual ad hoc verification pattern, used throughout F19-F24) does *not* process `project.godot`'s `[autoload]` section at all (confirmed empirically: `get_root().get_children()` is empty immediately after boot in that mode). Every earlier headless check in this project happened to not need an autoload, so this never came up before. Verified by instantiating a temporary root scene/script (discarded after use) instead of the usual `--script` override, confirming: `GameProgress` resolves and holds correct state; the full 7-node path is walkable forward node-by-node with the right input directions; a locked-frontier move is refused; walking one node back works; and all 6 non-finale levels correctly unlock the next level and set `last_played_level` on `_on_level_ingredient_collected()`.

A real caught mistake during this build: the first node layout used diagonal-ish deltas (e.g. node1->node2 was `(160, -170)`), where the dominant axis came out *slightly* vertical by only 10px -- the intuitive "press right to generally head right across the map" input didn't work, silently requiring "up" instead. Caught by the same headless walk-through, not by inspection. Fixed by redesigning the whole path as axis-aligned segments (each purely horizontal or purely vertical) specifically so which input direction advances is never ambiguous.

### F26 — Levels 3-6 Environment Art Pass (equivalent to F11)
The environment-art pass deferred since Phase 2 (see F12-F16), resumed once the Phase 4 maze redesign (F22) finalized L3-6's layout. Same 8-asset-per-level shape as F11 (obstacle small/medium/large, hazard fill, ground tile, 2 scenery decorations, window/parallax view), plus a real ingredient icon per level (`guac_ingredient`, `tomato_ingredient`, `sour_cream_ingredient`, `crunchy_shell_ingredient`, replacing the `Polygon2D` blobs in `GuacIngredient.tscn`/`TomatoIngredient.tscn`/`SourCreamIngredient.tscn`/`CrunchyShellIngredient.tscn` -- these are the same scenes Level 7's delivery ritual reuses, so the fix carries there too) -- 36 assets total. Per direct user instruction, every asset used `gemini-3-pro-image` from the start rather than F11's flash-then-regen detour.

**Themes:** L3 Guac Stand (avocado crates, guac hazard, wooden plank ground, lime/onion decorations); L4 Market Tomatoes (tomato crates, marinara hazard, cobblestone ground, chili string/awning bunting); L5 Frosty Fridge (frosty tub obstacles, icy-slush hazard, steel-fridge ground, icicles/frost puffs); L6 Sizzling Griddle (taco-shell crates, hot-grease hazard, griddle-metal ground, utensil rack/shell stack).

**Defects found and fixed on verification (visual review of every asset before wiring, per this project's standing practice):**
- **Character bleed into pure textures/props**, the exact F11 failure mode, on 2 assets despite the model upgrade: `l3_hazard_fill` (tiny chip/hot-sauce characters baked into the guac texture) and `l4_obstacle_medium` (characters standing in front of the crate). Fixed by regenerating with `NoRefs=true` added (these prompts had been sending character reference images despite not needing style-matching) and explicit "no characters/creatures" language -- both fixes were prompt-level, not a model problem.
- **Enclosed magenta pockets** (the other documented F11 limitation -- `crop-sprites.ps1`'s border flood-fill can't reach magenta fully enclosed by outline pixels) turned up on 9 assets this batch: crate slat gaps (`l3_obstacle_medium/large`, `l4_obstacle_large`), a bow's ribbon loops (`l3_lime_bunch`), a braid's tie (`l3_onion_braid`), stem gaps (`l4_chili_string`), a spatula's drainage holes (`l6_utensil_rack`), and larger un-keyed regions on `l5_window_view` (whole side panels) and `l6_window_view` (noise scattered through a botched "heat haze" effect, bad enough to warrant regenerating with the translucency language removed from the prompt rather than patched). Built a new one-off `tools/fix-enclosed-magenta.ps1` rather than hand-editing pixels: same hue/saturation match as the existing chroma-key, but scans for pockets not reachable from the border (redundant with the already-removed border-connected magenta) and can cap removal to small pockets only (`-MaxPocketPx`) so a legitimate warm-toned sky gradient isn't mistaken for a sliver -- the exact failure mode that got the earlier blanket-sweep attempt reverted in F11. Per that same standing rule, every fix was visually re-verified per-asset before trusting it, and unbounded removal was only used on the 2 files individually confirmed to have zero legitimate magenta-hued content.
- One cosmetic, non-technical judgment call left as-is: `l6_obstacle_large`'s "heat glow" rendered as a warm pink/rose halo (hue ~335°, distinct enough from the #FF00FF background that the chroma-key correctly left it alone) rather than the more expected orange/red -- a defensible reading of an admittedly ambiguous prompt, not a defect, so not worth another paid regen.

**A real rendering bug only caught by an actual screenshot, not headless instantiation:** all 4 levels' new `WindowParallax` background (built the same way as F11's, `BackdropLayer` with `motion_scale=(0,0)` holding a solid-color `ColorRect`) initially left a visible gray gap between the sky and the ground -- headless scene instantiation reported zero errors and the right child counts, since nothing was actually broken structurally. The bug: a `motion_scale=(0,0)` layer's child offsets behave as screen-space coordinates (must exceed the 1280x720 viewport, e.g. L1's proven `-700/-450/5127/750`), not world-space level bounds -- and this batch's `Backdrop` rects had been copied verbatim from each level's own *pre-parallax* top-level `ColorRect` (sized to cover the level's world-x span, the right approach for that older setup but wrong once nested inside a screen-locked parallax layer). Root-caused by instantiating a real scene in a real (non-headless) Godot process, taking an actual screenshot via `Viewport.get_texture().get_image().save_png()`, and sampling the gap's exact pixel color (`RGB(76,76,76)`, Godot's default clear color) to confirm nothing was drawing there at all. Fixed by matching L1's exact proven offsets across all 4 levels. Re-verified visually afterward (screenshots of all 4 levels, plus close-ups of Level 3's hazard-fill gap and boss arena) -- ground/hazard/obstacle/window/scenery all confirmed rendering in the right place at the right scale.

**Wiring notes:** obstacle collision heights stayed exactly F11's convention (32/46/58px small/medium/large, under the single-jump ~73.5px ceiling); each obstacle's `Sprite2D` scale is simply `target_collision_height / source_pixel_height`, computed per-asset since crop dimensions vary (unlike F11's L1/L2 batch, this batch's crops weren't uniform sizes). Ingredient icon scale follows the same ratio already established by `cheese_ingredient.png` (~33px displayed height against a 16px collision-circle radius). Window views target a consistent ~700px displayed width regardless of source crop size, tiled edge-to-edge four times per level. The two hazard gaps are identical across all of L3-6 (x 500-590 and 1300-1390) since F22 confirmed all four levels share the same ground-segment template.

**Deliberately out of scope, matching the confirmed asset list:** the F22 two-tier maze walkways (`UpperTier1-4`/`UpperAccess1`) and the F19/F21 bonus-platform pedestals remain plain gray `Polygon2D`s -- neither was part of the prompt batch confirmed with the user before generation, and retexturing them wasn't asked for. Scenery decoration placement (lime bunches, onion braids, icicles, etc. -- 3 instances each, spread across the level near thematically-matching enemies) was a layout judgment call with no exact spec to follow, similar to F11's own cactus/string-light placement; worth a look during the eventual full playtest but not blocking.

**This closes the last open item in the Full 7-Level MVP's Definition of Done** -- every level now has real generated art, not procedural placeholders. What's left project-wide is the still-pending full interactive playtest (see Open Questions).

### F27 — Cornchip Spin/Slide/Run Poses and Wrap's Fullness Progression
Direct user request ("give all the characters more animations... spin slide etc... this way the main character looks better"), scoped down to Cornchip + Wrap after clarifying: spin-dash and ice-sliding had *no* dedicated pose at all before this -- both states were just a color-tint modulate over whatever idle/run/jump frame happened to be playing. Wrap also played a single held "idle" pose through the entire Level 7 delivery sequence, only switching pose at 6/6.

**Cornchip — 4 new poses** (`cornchip_extras_grid`, cropped to `cornchip_extras_frame1-4.png`): a coiled/tucked pose for spin-dash, a leaning ice-skid pose, and 2 new mid-stride "passing position" run frames.
- **Spin-dash** (`player.gd`): the new pose is a single static image -- the actual spinning motion is Cornchip's sprite being continuously rotated in code (`SPIN_ROTATE_SPEED = 900 deg/sec`, reset to 0 in `_end_spin()`), not a multi-frame animation. `_update_animation()` now gives `is_spinning` top priority over run/jump/idle.
- **Ice-slide**: a new `"slide"` animation, shown whenever `is_on_ice` is true and Cornchip is actually moving (`absf(velocity.x) > 1.0`) -- second priority after spin, ahead of jump/run.
- **Extra run frames**: the existing 2-frame run cycle (`tex_run1`/`tex_run2`) became a 4-frame cycle (`run1 -> extras3 -> run2 -> extras4 -> loop`); `speed` doubled from 8.0 to 16.0 to keep the same overall stride rate now that there are 2x the frames per cycle, rather than the run cycle appearing to slow down.

**Wrap — 3 new poses** (`wrap_extras_grid`, cropped to `wrap_extras_frame1-3.png`): increasingly stuffed-with-ingredients poses, replacing the old single-state "idle the whole time, smile at 6/6" behavior with a visible fullness progression as deliveries land.
- `wrap.gd` gained `_update_fullness_visual()`, called after every delivery that doesn't complete the ritual: `filling1` at >= 1/3 of `required_deliveries` (2 by default), `filling2` at >= 2/3 (4 by default) -- thresholds are proportional to the export rather than hardcoded so they stay sensible if `required_deliveries` is ever retuned.
- The old `"celebrate"` animation's texture (`wrap_frame8.png`, "a warmer, softer smile" with no visible fullness) was swapped for the new fully-stuffed-and-smiling pose (`wrap_extras_frame3.png`) -- same animation name and node wiring in `wrap.gd`/`WrapBoss.tscn`, just a better pose for the same narrative beat, so no code changes were needed beyond the scene resource swap.

**Pipeline work needed to make new poses for an *existing* character safe:** both Cornchip and Wrap's existing frames are normalized onto a fixed per-character canvas (292x256 and 225x299 respectively, from `crop-sprites.ps1`'s original per-grid normalization) -- a naive new generation would land on its own, different canvas size, and the character would visibly resize the instant the game switched to one of the new poses.
- `generate-sprites.ps1` gained an optional `ExtraRefs` field (character-specific reference images, e.g. `wrap_frame1.png`, added on top of the fixed cornchip/hot-sauce style refs) so new poses for an established character match that character's specific design, not just the general house art style.
- `crop-sprites.ps1` gained `$targetCanvasOverrides` (grid basename -> path to that character's existing `frame1.png`, read for its pixel dimensions) plus a fit-and-downscale step for any individual pose that comes back larger than the target canvas. Hit a real path bug on the first run (missing the `Assets\generated\` prefix, so the override silently fell back to the grid's own size) -- caught immediately since the resulting frames were the wrong size, but it had already deleted the source grid images (the slicer's `Remove-Item $path` at the end of a successful-looking run), forcing a full regeneration rather than a simple re-crop. Fixed and re-verified: both grids now correctly produce frames at their exact target canvas size.
- **Two content misses caught on the first generation and fixed via a revised prompt + regeneration, not accepted as "close enough":** Cornchip's spin pose first came back as a generic round cookie (broke the established triangle silhouette entirely) -- fixed by describing a "still clearly the same upside-down-triangle-shaped character, not a circular or ball shape" coiled crouch instead of asking for a "curled into a ball" shape outright. Wrap's fullness poses first came back as subtle bumpy-texture variation, too weak to read at actual gameplay sprite scale -- fixed by asking explicitly for "distinct round ingredient bulges... like a snake that swallowed a few eggs," which reads clearly even scaled down.
- **Verified via real rendered screenshots** (same technique as F26's backdrop-bug catch): instantiated `Player.tscn`/`WrapBoss.tscn` standalone, forced each state (`_start_spin()`, `is_on_ice` + nonzero velocity, each Wrap fullness threshold), and confirmed all render at consistent scale with no visible resize glitch between animations.

**Follow-up built 2026-07-21 (was flagged here as a separate pass):** the spin-dash cooldown from `plan.md` Phase 5's FB19 -- see F30.

### F28 — Animated Hazard-Fill Textures (Phase 5)
"Hazard-fill textures should visually animate (movement/flow, like real bubbling lava/cheese/etc.), not sit static" -- from the full playtest.

- New `scripts/shaders/hazard_flow.gdshader`: a `canvas_item` shader that scrolls the texture's UV over `TIME` (tunable `scroll_speed`) plus a small sinusoidal horizontal wobble, wrapped with `fract()` so it tiles seamlessly within each hazard fill's cropped view rather than showing a hard seam.
- One shared `Assets/materials/hazard_flow.tres` `ShaderMaterial` (not a separate instance per node) referenced as a single `ExtResource` and assigned to all 14 `HazardFill*` `TextureRect` nodes across Levels 1-6 -- stateless shader (only reads global `TIME`), so sharing is safe and avoids 14 duplicate resources.
- **Verified via a headless instantiate check** (all 6 levels load with every `HazardFill*` node's `material` resolving to a real shader) **and an actual rendered screenshot diff** (two frames 40 ticks apart of Level1's salsa hazard fill, confirming the bubble pattern visibly shifted, not just that the material was attached).

### F29 — L3/L4/L5 Window-View Art Fix (Phase 5)
Root-caused, not just regenerated blind: the full playtest called L3's window view "a broken mess" and L4/L5 "not right," despite F26 passing this session's own visual QA.

- **Root cause:** comparing against L1/L2/L6 (which all work) showed those three prompts were structured as "[opaque themed wall], with a [window/frame] showing a hazy scene" -- explicitly describing a wall that fills the whole canvas. L3/L4/L5's original prompts only described a *frame* ("market-stall awning frame," "fridge-shelving frame") with no wall behind it. Since these assets use the magenta chroma-key pipeline (`crop-sprites.ps1`), anywhere the model left magenta (because nothing in the prompt told it to fill that area) got cropped to transparent. L3's prompt was vague enough the model didn't even draw a frame -- it drew a full unrelated market-stall scene, so cropping left disconnected floating shapes with no anchor. L4/L5 kept a frame shape but with an unthemed background (hot-pink sky + random city skyline for L4; vague cloud shapes for L5) showing through the ungrounded portions.
- **Fix:** rewrote all 3 prompts in `generate-sprites.ps1` using the proven L1 template ("a [themed material] wall, filling the entire canvas edge-to-edge with no gaps or empty background, with a [window/opening] showing a hazy distant scene..."), regenerated via `gemini-3-pro-image`.
- **Real defect found and manually repaired, not blanket-swept:** L3's regenerated art still had leftover magenta chroma-key slivers in the plank grooves (the same class of bug documented in Phase 1 -- `crop-sprites.ps1`'s border-flood-fill misses magenta pockets enclosed by outline pixels). First inpainting attempt (dilate-fill from non-magenta neighbors) used too strict a color test (`R>180`) and missed the darker anti-aliased fringe pixels between the bright magenta core and the black outline, leaving a muddy purple residue. Fixed with a hue-based test (G channel low relative to *both* R and B, regardless of absolute brightness) that correctly separates true magenta/purple slivers from the wood's legitimate brown tones and the outline's near-black -- verified with a full-image pixel scan (0 magenta-ish pixels remaining) before installing the fixed file.
- **Real scale bug caught before shipping:** the new art has different native pixel dimensions than the old broken art (e.g. L3: 755x442 -> 1294x710), but the scene files' `Window1-4` `scale` values were still tuned to the old dimensions. Recomputed each level's scale from `700 / new_native_width` (matching the existing tiling convention -- displayed width must equal the 700px window spacing for seamless edge-to-edge tiling) and updated `Level3/4/5.tscn`.
- **Verified via headless instantiate + real rendered screenshots** of all 3 levels' window backgrounds, confirming seamless edge-to-edge tiling with no visible seam, gap, or leftover chroma-key artifact.
- **Not done:** L4's "bring the background down to ground level" and L5's "enlarge + reposition" asks were about camera/parallax *positioning*, not art content -- out of scope for this fix, flagged in `plan.md` for a follow-up if still wanted. Per-level-distinct maze-tier textures (L3-6 share one texture) and L6's window-copy variety were also raised in the same feedback but **declined by the user** ("stop wasting my money... move onto the non-aesthetic changes") before being built -- this is where the session's paid-art spend stopped.

### F30 — Gameplay Balance & Containment Pass (Phase 5)
Four small systems fixes, built together as one batch of pure code/design changes (no art) after the user redirected effort away from further paid generation.

- **Spin-dash cooldown (FB19):** `player.gd` gained `SPIN_COOLDOWN = 1.5s` and `spin_cooldown_remaining`, set on `_end_spin()` and gating the next `_start_spin()` trigger -- previously the ability could be re-triggered the instant `SPIN_DURATION` expired, making the enemy-defeat window spammable into near-permanent invincibility.
- **Enemy free-roam (FB20):** `boss.gd` (shared by all 15 enemy/boss scenes) replaced its deterministic "walk to the patrol boundary, flip, walk back" metronome with a randomized timer (`ROAM_MIN/MAX_INTERVAL`, `ROAM_PAUSE_CHANCE`) that picks a new direction or a brief pause, layered on top of -- not replacing -- the existing hard safety checks (leash bounds via `patrol_half_width`, platform-edge raycast, wall-collision turn-around). **Verified via a headless simulation** driving 30 real simulated seconds of a live enemy's `_physics_process` through the actual running engine loop (a first attempt manually stepping physics outside the real frame loop deadlocked on a physics-space query and had to be redone) -- confirmed it stays within its leash and shows real variety (8 direction changes, ~1.85s of paused time).
- **Camera pit-fall limit + level boundary walls:** `level_base.gd` gained `_setup_level_bounds()`, called from every level's `_ready()`. Derives the level's real left/right/floor extents by scanning its own `Ground*`-named `StaticBody2D` pieces (no per-level hand-authoring, can't drift out of sync) and spawns two invisible `StaticBody2D` boundary walls just outside those bounds (blocking the player from walking off either end). **Real bug caught and fixed during verification:** both walls were built via a shared helper that set `node.name` before `add_child()` -- worked for the first wall, but the second silently got Godot's auto-generated `@StaticBody2D@N` name instead of the intended dedup, so a check for `"BoundaryWall"`-prefixed children only ever found one. Fixed by passing each wall an explicit distinct name instead of relying on collision auto-rename. Verified across all 7 levels via headless script (sane per-level bounds, spawn position always inside them).
- **Camera pit-fall limit — shipped, then reverted as a live regression the very next message.** The original approach also set `Camera2D.limit_left/right/bottom` from the same ground-piece scan, meant to cap the pit-fall dip. **Real bug, caught by the user in live play, not by any test:** this project's `Camera2D` uses a non-default `offset` (`Vector2(150, -225)`, see `Player.tscn`), and Godot's limit clamping interacts with that offset in a way that shifted the camera's resting position by a large amount on *both* axes -- not just at the edges, but during completely ordinary standing play at the level's own spawn point, pushing the player off the top of the screen entirely in every level. Confirmed via a headless screenshot comparing clamped vs. unclamped `Camera2D.get_screen_center_position()` (105 unclamped vs. -105 clamped, at a spot nowhere near any actual boundary). Reverted the limit assignments; the boundary walls (physical, no camera involvement) fully cover the actual "can't walk off the level" requirement on their own. **Lesson:** Godot's `Camera2D` limits interacting with a non-default `offset` weren't real-play-tested before shipping (only asserted numerically) -- this class of bug needs an actual rendered screenshot at normal gameplay position, not just at the edges being tested for.
- **Life-pickup scarcity, sky-drop:** "the life should be harder to get and possibly should just drop from the sky when you are down to 1 life" -- `level_base.gd`'s `_on_player_hit()` now spawns a `LifePickup` above wherever the player currently is (the existing rolling-checkpoint means that's also roughly where they'll respawn) whenever lives drops to exactly 1, tweened falling to rest on the ground. The existing static per-level bonus pickups (already gated behind a jump-challenge platform, per F19/F21) were left as-is rather than blindly re-tuned. **Verified end-to-end via headless script:** confirmed the pickup spawns, falls, and (landing right where the player is) gets auto-collected, taking lives from 1 back to 2.

### F31 — Bean System & Ingredient "Shopping Basket" UI (Phase 5)
FB13 (broadened) and FB17, built together since both extend `level_base.gd`'s HUD and `ingredient.gd`'s shared collect flow.

- **HUD bean counter:** `level_base.gd` builds a bean icon + `Label` (`"collected/total"`) under each level's `HUD` at runtime (not hand-added to all 7 scenes), updating live via the existing `bean_token` group's `collected` signal.
- **Sparkle-on-collect:** `ingredient.gd` gained an opt-in `sparkle_on_collect` export (default off, so every other reuse of this shared script -- ingredients, life pickups, wrap deliveries -- is unaffected), enabled only on `BeanToken.tscn`. Spawns a one-shot `CPUParticles2D` burst on collect, parented to the current scene (not `self`, which is about to `queue_free()`).
- **Persisted high score:** `GameProgress` (previously explicitly in-memory-only, see F25) gained its first disk-persisted stat: `total_beans_collected`, saved to `user://progress.save` via `FileAccess.store_var()`/`get_var()` on every bean collected. Deliberately scoped narrower than the rest of `GameProgress`'s state (`highest_unlocked_level`/`last_played_level` stay in-memory per the existing confirmed design) -- beans are a genuine cross-session tally, level-unlock progress isn't.
- **Ingredient "shopping basket" checklist (FB17):** a row of the 6 boss-drop ingredient icons in the HUD (top-right corner, anchored to the viewport edge rather than a hardcoded width), dimmed until that level's ingredient is actually collected. Collected state is derived from `GameProgress.highest_unlocked_level` (`level_number < highest_unlocked_level`) rather than tracked as separate state that could drift out of sync -- `unlock_level(N+1)` only ever fires from actually collecting level N's ingredient. On collect, `_fly_ingredient_to_hud()` flies a copy of the ingredient's own sprite from its world position to its checklist slot, lighting the slot up on arrival -- the "more fanfare... fly into a shopping basket" from the raw feedback.
- **Real bug caught and fixed:** `Camera2D.unproject_position()` doesn't exist in Godot 4 (that's a Camera3D-only method) -- the correct world-to-screen conversion for a 2D viewport is `get_viewport().get_canvas_transform() * world_pos`. Caught via headless test before it ever shipped.
- **Verified via headless script** (checklist icon starts dimmed, flips to full color exactly when the fly tween completes, `GameProgress.highest_unlocked_level` advances correctly) **and a real rendered screenshot** of the HUD layout (bean counter + checklist both legible, no overlap with the existing life icons).

### F32 — Air Fryer Onboarding (Phase 5, FB18 partial)
"When you collect the airfryer it should fly up to the top right and a logo for the ability will appear."

- `level_base.gd` gained `get_ability_hud_slot()`, lazily creating a single persistent (initially invisible) HUD icon slot the first time any pickup-granted ability needs one -- generic for any future ability pickup, not Air-Fryer-specific.
- `air_fryer.gd` rewritten: instead of a scale-and-fade-in-place tween, it now flies a copy of its own icon from its world position to that HUD slot, lighting the slot up on arrival -- same technique as F31's ingredient checklist.
- **Real bug caught and fixed:** the fly tween was created via `create_tween()` on the Air Fryer node itself, which was then immediately `queue_free()`'d -- a `Tween` bound to a node's lifetime stops early if that node leaves the tree, so the tween silently died before completing and the HUD slot never lit up. Fixed by binding the tween to the persistent `flying` icon node instead. Caught via headless test (slot modulate still showed alpha 0 a full second after triggering pickup) before it shipped.
- **Verified via headless script** (ability granted, slot created, modulate transitions from invisible to opaque exactly on tween completion) **and a real rendered screenshot** sequence (mid-flight + landed).
- **Not done:** the "first-time paused explainer" half of FB18 -- scoped down to the concrete, literally-requested "fly to HUD + icon appears" behavior. The explainer wasn't in the user's own raw feedback (it was this project's own elaboration when triaging the backlog), and the project's no-reading-required design pillar rules out a text-based explainer anyway; a non-text version wasn't pursued to avoid speculative scope on top of an already-large batch.

### F33 — L2 Climbing Bonus Section (Phase 5)
"It would be good to make a cornchip climbing part for either a special powerup or a free life. (We already have the sprites.)" -- the sprite claim was checked, not assumed: no climbing-specific Cornchip pose exists anywhere in the generated art. Built anyway with a flagged placeholder, per direct instruction, rather than blocked on new art or silently substituted without a note.

- **New player state** (`player.gd`): `in_climb_zone`/`is_climbing` plus `CLIMB_SPEED`. Grabbing a ladder requires pressing up/down *while* overlapping a `ClimbZone` (not just touching its edge, which would otherwise suck the player onto it mid-jump) -- entering the zone only arms the grab. While climbing, gravity is bypassed and vertical velocity is fully input-driven; jumping cancels the climb and pops off with a normal jump. Since spin-dash also binds to "up," the spin-dash trigger explicitly excludes `in_climb_zone` so the two can't fight over the same input.
- **New `ClimbZone.tscn`/`climb_zone.gd`:** an `Area2D` that toggles the player's climb-grab availability on enter/exit, with a procedural ladder visual (`Polygon2D` rails + rungs, flat-vector style matching the game's existing look) -- zero new art generation.
- **Placed in `Level2.tscn`** in a genuinely clear stretch of `GroundB` (x=1050-1300, clear of `JumpObstacleLarge1` and the neighboring hazard gaps), leading up to a bonus platform (reusing the existing `RectangleShape2D_bonus_platform` shape/`BonusPlatform` pattern already proven elsewhere) holding a `LifePickup` -- the "free life" reward option from the raw feedback.
- **Flagged sprite mismatch:** `_update_animation()` reuses the existing "jump" pose while climbing, with an explicit code comment noting no dedicated climbing pose exists -- a deliberate, visible substitution rather than a silent one.
- **Verified via headless script:** teleported the player into the zone, held "up" for ~1.25 simulated seconds through the real engine loop, confirmed `is_climbing` engaged and the player moved the expected distance at `CLIMB_SPEED` **and a real rendered screenshot** confirming the ladder renders as a recognizable climbable structure.

### F34 — Remaining Phase 5 Art/Polish Batch (L6/L7, World Map, Title/Ending Screens, HeatZone)
Built 2026-07-21 after the user reopened paid art generation ("do it all... without my input"), following the F30-F33 systems batch and its camera-limit regression (caught live and reverted the same session, see F30's note above).

**L6 window variety:** 2 new variant textures (`l6_window_view_b`/`_c`) generated and alternated across the 4 `Window1-4` sprites (Window1/3 keep the original, Window2 = `_b`, Window4 = `_c`), breaking up the "4 copies of the same kitchen scene" repeat. **Real defect caught and fixed before shipping:** `_c`'s first generation left its background wall as raw unfilled magenta (same class of bug as F29's window-view fix, just discovered independently on a different asset) -- regenerated with a reinforced "must be completely filled, no magenta" prompt.

**Level 7 environment art:** the level had zero real art before this (flat `ColorRect`s for backdrop and every ground segment). Generated `l7_ground_tile` (seamless steel counter texture), `l7_window_view` (a golden serving-counter wall with a pass-through opening, tiled across 6 `Window1-6` instances to cover the level's full length), and 2 decorative props (`l7_napkin_stack`, `l7_condiment_rack`). **Two real defects caught and fixed before shipping, not accepted as "close enough":** the window-view background also came back with an unfilled magenta wall (fixed with the same reinforced-prompt technique as L6 above); the condiment-rack prop had leftover chroma-key slivers in the enclosed gaps between bottles under its crossbar. The rack fix deliberately used a *different* technique than F29's plank-groove repair: those gaps are genuine open space (should read as transparent, not wood-shadow), so the fix was a direct alpha=0 cut on the classified magenta pixels rather than inpainting a color that wouldn't belong there -- verified by rendering both the "inpaint" attempt (produced a wrong-looking dark smudge) and the corrected "cut" version side by side before choosing the cut.
Also addressed the "suspended blocks" part of the L7 feedback: all 6 delivery ingredients moved from sitting flat on the ground onto individual pedestals (reusing the exact `RectangleShape2D_bonus_platform` geometry already proven jump-safe by F19/F21 -- a 60px rise, well under the 73.5px single-jump apex), with the ingredient itself resting 55px above the pedestal top, mirroring the already-shipped `BonusPlatform1`/`BonusLifePickup1` pattern exactly. **Not done:** spreading out the F24 ice/guac/chase corridor -- would require widening `GroundC`'s collision/visual and re-verifying hazard/detour spacing, judged higher-risk for a vaguer ask than the suspended-blocks change (which had a concrete, provably-safe geometry to reuse).
`generate-sprites.ps1`'s `Get-AssetSubdir` gained `l7_*` -> `environment\level7` and `ui_*` -> `ui` routing rules (previously missing, so the first `l7_ground_tile` generation landed in the flat `Assets/generated/` root before the fix).

**World map redesign (FB16):** generated `ui_world_map_bg` (a full-bleed illustrated map background, winding path through a cartoon market town) as `WorldMap.tscn`'s new backdrop, replacing the flat `ColorRect`. The 7 level nodes' plain diamond `Polygon2D` markers were replaced with `TextureRect` icons showing that level's *own already-generated* ingredient art (lettuce/cheese/guac/tomato/sour-cream/crunchy-shell for L1-6, Wrap's own idle frame for L7) -- zero extra generation needed, since the game already had a strong per-level visual identity via its collectible icons. Locked nodes get a small padlock built procedurally at runtime (`world_map.gd`'s `_build_lock_icon()` -- a `Polygon2D` body + a `Line2D` arc shackle) rather than a new generated asset. Added the reused "Corn Chip Day" title text and a bean-count display (icon + `GameProgress.total_beans_collected`, same pattern as the in-level HUD from F31).

**Title screen polish:** see `title_screen.gd` -- idle now holds the plain open-eyes pose (frame 0) almost the entire time, cutting to the existing wink pose (frame 1) for only `BLINK_DURATION = 0.15s` on a randomized `2.5-5.0s` timer, instead of the old continuous 0.8s-cycle loop between the two frames. Reuses the same 2 existing frames -- no new art. **Verified via headless sampling:** frame 0 held ~98% of sampled frames over an 8-second run. The down-pointing `PromptIcon` triangle was redrawn as a rightward "play button" shape (`PackedVector2Array(-10,-14, -10,14, 14,0)`), a more universal press-to-continue symbol than an arrow that happened to point the wrong direction for what it meant.

**Ending screen (FB21):** new `EndingScreen.tscn`/`ending_screen.gd`. Deliberately wordless per the no-reading-required pillar -- Cornchip's existing `celebrate` pose (`cornchip_frame8.png`) and Wrap's existing fully-stuffed pose (`wrap_extras_frame3.png`) stand together, with a procedural `CPUParticles2D` confetti burst (a real bug caught before shipping: `emission_shape` was set to `1`, which is `SPHERE`, not `RECTANGLE` (`3`) -- produced a narrow vertical confetti column instead of spreading across the screen; fixed and verified via screenshot). Reuses the title screen's "Corn Chip Day" text and play-button prompt rather than introducing new wording. `player.gd`'s `win_celebration()` now holds the in-place celebration for 1.5s (matching the existing level-complete beat) before handing off via `change_scene_to_file`, instead of freezing forever in `Level7`.

**HeatZone texture:** `IceZone.tscn` and L7's ice/guac patches already used real floor textures from an earlier session (`l5_ground_tile.png`/`l3_hazard_fill.png` respectively) -- only `HeatZone.tscn`'s circular `Visual` `Polygon2D` was still a flat color fill. Added `texture = l6_ground_tile.png` to the polygon; `heat_zone.gd`'s existing safe/warning/hot `color` cycling (unchanged) now tints real texture detail instead of a flat wash. **Real bug caught and fixed before shipping:** an initial `texture_scale = Vector2(0.005, 0.005)` produced a flat-looking clamped-edge-pixel result (no visible texture detail) -- Polygon2D's automatic UV mapping needs roughly a 1:1 scale for a texture of this size against a ~340-unit polygon, not a manually-guessed small multiplier; removing the explicit scale (default 1.0) fixed it, confirmed via screenshot showing real diagonal grill-line detail.

**A real regression shipped and fixed this session, worth flagging here too (see F30's own note for the full story):** the camera pit-fall limit from F30 broke normal camera framing in every level (the player was pushed off the top of the screen at ordinary standing height, not just at level edges), caught by the user in live play within the same session. Root cause: `Camera2D.limit_left/right/bottom` clashing with the project's non-default `Camera2D.offset`. Reverted; the boundary walls (unaffected, no camera involvement) still stand on their own.

### F35 — Tomato Power-Up (FB11)
`TomatoPowerup.tscn`/`tomato_powerup.gd`: a standalone timed-ability pickup, placed once in `Level4.tscn` (distinct from `TomatoIngredient.tscn`, Level 4's boss-drop ingredient -- same fly-in collect pattern as every other pickup, no new visual language). Grants `player.gd`'s `has_tomato_power` for `TOMATO_POWER_DURATION = 8.0`s, with a warm-red sprite tint (`TOMATO_TINT`) as the "ability active" cue, matching the Air Fryer's existing modulate-tint convention. While active, pressing down (`ui_down`, already free -- up is spin-dash, left/right/space are movement/jump) fires a `SeedProjectile.tscn` in the facing direction, gated by its own cooldown so it can't be spammed into a laser.
- `seed_projectile.gd` mirrors `boss_projectile.gd`'s lifetime/self-free pattern but in the opposite direction: it calls `take_ranged_hit()` on anything that has the method, rather than hurting the player.
- `boss.gd` gained `take_ranged_hit()`, deliberately **not** a blanket "defeats anything" rule -- it respects each boss's existing F18 vulnerability gating (Queso Grande still only falls to spin-dash, Hot Sauce/Avocado still only take a hit during their attack-tell window, Iron Skillet still only takes a hit during the cool phase), so the power-up is a ranged option within the same fight rules already established, not a way to bypass them.
- **Two real bugs caught and fixed while verifying, before shipping:** (1) fired seeds spawned exactly at the player's own position and immediately self-collided with the firer before ever reaching a target -- fixed by spawning offset ahead in the fire direction. (2) `level_base.gd`'s FB30 sky-drop life pickup could hit Godot's "Can't change this state while flushing queries" error when triggered from inside an enemy's own `Area2D.body_entered` callback (an enemy death during a seed fight can coincide with the player being at 1 life) -- fixed by deferring the pickup's `add_child()` + setup as one deferred call instead of doing it synchronously mid-signal.

### F36 — Procedural SFX Pass (FB5, partial)
"Audio/music/SFX pass (ambient sound is fine — still no reading required)" -- this pass covers the SFX half only; ambient/music is still open (see Backlog FB5).
- New `AudioManager` autoload (the project's second, after `GameProgress`) -- `scripts/audio_manager.gd`, a small pooled-`AudioStreamPlayer` (`POOL_SIZE = 6`) so overlapping triggers (e.g. two beans grabbed the same frame) don't cut each other off. Any script calls `AudioManager.play("jump")` by name rather than needing an `AudioStreamPlayer` node wired into every scene.
- 5 sounds: jump, bean_collect, hit, life_pickup, level_complete -- wired into `player.gd` (jump/hit), `ingredient.gd` (collect, shared by beans/ingredients/life-pickups so `life_pickup` needed its own branch), and `level_base.gd` (level_complete).
- **No audio-generation API is configured in this project** (only Gemini, for images) -- rather than skip this or fake it, all 5 are synthesized directly in Godot via a one-off `tools/generate-sfx.gd` (sine/square waves with amplitude envelopes, saved as `.tres` `AudioStreamWAV` resources under `Assets/generated/audio/`), needing no new tooling or paid generation.
- Real music/ambient tracks are explicitly a different, higher bar (must not sound annoying on a tight loop) and were left out of this pass -- see Backlog FB5. **Closed out 2026-07-21, see F37.**

### F37 — Ambient Background Loop (FB5, remainder)
Closes out FB5. Same no-audio-API constraint as F36, and deliberately conservative given music is a higher bar than SFX ("must not sound annoying on loop"): a single quiet, static-harmony pad (C-E-G-D add9, `tools/generate-music.gd`), no melody or percussion, nothing rhythmic to get grating on repeat. `AudioManager` (already an autoload, see F36) starts it once in its own `_ready()` at `-16 dB` -- persists unchanged across every scene change (title, map, all 7 levels) with zero per-scene wiring, matching the original "ambient sound is fine" scope (one continuous mood layer, not per-level distinct tracks -- that's a bigger ask than what was asked for).
- **Seamless-loop technique:** every oscillator's frequency is snapped to complete a whole number of cycles across the 16s loop duration (`freq = round(target_hz * duration) / duration`) -- a whole-cycle-count sine has identical value *and* slope at both the start and end of the loop, so `AudioStreamWAV`'s native `LOOP_FORWARD` (`loop_begin=0`, `loop_end=`full sample count) is genuinely click-free at the seam, not just faded to hide one. The amplitude "breathing" LFO uses the same trick (exactly 1 cycle over the loop) so the whole thing, including its slow swell, loops with no audible seam.
- **Verified:** the saved `.tres` resource's `loop_mode`/`loop_end`/`mix_rate` fields inspected directly to confirm the loop metadata matches the design (`loop_mode=1` i.e. `LOOP_FORWARD`, `loop_end=352800` = exactly `22050 * 16`); a real (non-headless) boot into `TitleScreen.tscn` produced zero errors with the new player wired in. **Not verified: whether it actually sounds pleasant** -- unlike every other audio item in this project, there's no way for the AI to hear the result: this needs the user's own ears, same open caveat as F36's SFX. Trivial to disable (comment out the `_ambient_player.play()` call) if it doesn't land.

### Real bug found and fixed via live play, not headless testing (2026-07-21)
While doing a post-Phase-5 verification pass (booting the real game to screenshot-check the batch of art/systems that had only been headless-verified before), a real crash surfaced during actual play: **"Can't change this state while flushing queries"** fired from `boss.gd`'s `_die()` (spawning the dropped ingredient, and separately spawning Big Red's split pieces) and from `obstacle_trigger.gd` (spawning a triggered obstacle) -- both call `add_child()` on a brand-new Area2D/StaticBody2D-based node directly from inside an `Area2D`'s own `body_entered` signal handler, the exact same root cause FB30's sky-drop life pickup already hit and fixed once (`level_base.gd`'s `_finish_sky_life_drop`). That fix was never generalized to the other two call sites, which is why it resurfaced. Fixed both the same way: instantiate first, then defer the actual `add_child()` + position/registration setup as one `call_deferred()`'d unit (`boss.gd`'s new `_finish_spawn()`/`_finish_ingredient_spawn()`, `obstacle_trigger.gd`'s new `_finish_spawn()`). This is a real, live-reproducible bug that would hit any player who spin-dashes or stomps a boss to death, or walks past an obstacle trigger -- not a hypothetical. Audited every other `add_child()` call in the codebase for the same pattern (new physics-shape-owning node, called synchronously from inside an `Area2D` signal handler): the only other candidate, `player.gd`'s `start_carrying()` (reparents an existing Area2D into the player on touch), was left alone -- it's a reparent of an existing node rather than a brand-new one entering the tree, and it's already been exercised extensively by F23's end-to-end delivery-ritual verification with no reported crash, so changing it without a concrete repro would be a speculative fix, not a confirmed one.
**Verification limitation, stated plainly:** confirmed via a real windowed boot with zero errors on idle observation, and the fix directly mirrors an already-proven pattern from the same codebase -- but reliably re-triggering the exact original crash (a live spin-dash boss kill) needs real player input, which this project's headless harness still can't simulate (the same limitation noted for movement-feel checks throughout this doc). Worth a direct eye during the next real playtest.

### F38 — 2-Player Local Co-op Prototype with Cheeto (FB6, partial)
Follows directly from FB6's 2026-07-21 investigation (see Backlog entry below), once the user resolved its two real blockers: Cheeto's visual design (cheese-puff character, see `characters.txt`) and the shared-lives/shared-moveset design fork.
- **Cheeto art:** generated via the same pipeline as every other character (`generate-sprites.ps1`'s `cheeto_grid`, 8-pose grid, cornchip/hot-sauce reference images for house-style consistency) -- a chunky curved cheese-puff silhouette, distinct from Cornchip's triangle and Wrap's tube. One real defect caught and fixed before use: `cheeto_frame1.png` had a leftover magenta sliver on the left leg (the known enclosed-pocket chroma-key gap `crop-sprites.ps1` already documents as a manual-fix case) -- inpainted by hand, verified visually clean after.
- **`player.gd` is now shared by both characters**, not forked: `input_left/right/up/down/jump` and `SPRITE_SCALE` are now `@export`s (default to the original `ui_*` actions and `0.22` scale, so Cornchip/solo play is byte-identical to before) rather than hardcoded constants, and `camera`/`camera_base_offset` are now null-safe since Player2 doesn't own a camera. This is the "shared moveset" decision made real in code -- one script drives both players.
- **Input:** `project.godot` gained its first-ever custom `[input]` actions -- `p2_left/right/up/down` (WASD) and `p2_jump` (E) -- registered alongside the untouched `ui_*` defaults Player1 still uses.
- **Camera decoupled from Player.tscn**, per the investigation's own finding: new `scripts/co_op_camera.gd`, a level-owned `Camera2D` that tracks both players' midpoint with distance-based dynamic zoom-out (simple shared-screen co-op, not split-screen). `level_base.gd`'s new `_setup_coop_camera()` only runs when a `Player2` node exists in the scene -- it disables both players' own `Camera2D`s and swaps in the shared one. Confirmed via headless script that solo levels (e.g. `Level1.tscn`, no `Player2` node) are completely unaffected: `Player`'s own camera stays `enabled = true`, no co-op camera is created.
- **Shared 3-life pool:** the confirmed design ("both players draw from one pool") fell out of the existing architecture for free -- `level_base.gd` already had one `_on_player_hit` handler wired to Player1's `player_hit` signal; connecting Player2's `player_hit` to that same handler is the entire implementation, no new life-tracking code needed.
- **New `scenes/Player2.tscn` and `scenes/CoOpTest.tscn`:** deliberately NOT wired into `Level1.tscn` or any of the other 6 shipped, already-playtested levels -- doing so would force co-op mode (camera zoom-out, a second character) onto every existing solo playthrough with no way to opt out, since there's no game-mode menu yet. `CoOpTest.tscn` is a small standalone level (flat ground, a few beans, one obstacle, the Air Fryer pickup) built specifically to verify the mechanics land correctly before deciding whether/how to roll co-op into the real 7 levels.
- **Verified:** headless script confirms both players spawn in group `"player"` (so existing bean/ingredient/hazard code picks Player2 up for free, per the original investigation's prediction), the co-op camera is created with both targets wired and both players' own cameras disabled, boundary walls still generate correctly, and `lives == 3` shared at start. A real windowed screenshot of `CoOpTest.tscn` confirms both characters render correctly on-screen with the shared life/bean HUD. Separately confirmed `Level1.tscn` (no `Player2` node) is byte-for-byte unaffected: its own camera stays enabled, zero co-op camera nodes created.
- **Known limitations, stated plainly:** (1) Cheeto's `SpriteFrames` has no dedicated spin-dash/ice-slide poses (that 8-pose grid never included them, same as Cornchip originally didn't until `cornchip_extras_grid` added them later) -- `spin`/`slide` reuse an existing jump/run frame as a stand-in, same precedent as FB33's climbing-pose reuse, so picking up Air Fryer or standing on ice won't crash but won't look bespoke either. (2) Onion's screen-wobble nuisance effect and any other `get_first_node_in_group("player")` call (currently just that one, in `boss.gd`) only ever targets whichever player is first in the group -- effectively Cornchip -- not the nearer player; a minor, documented gap, not a crash risk. (3) **No actual two-controller human playtest has happened** -- verification here is structural (headless) and visual (one screenshot), not felt co-op gameplay; an AI can't press two input devices at once. (4) Not rolled into any of the 7 real levels yet -- see `CoOpTest.tscn`'s own note above for why that's a deliberate, separate decision pending a real playtest of this prototype.

## Backlog (Post-MVP, Not Yet Scheduled)
- **FB1** — Ability-gated upgrade system spanning all 7 levels
- ~~FB2 — Remaining 6 levels~~ — Levels 2-6 built, see F10/F12/F13/F14/F15.
- ~~FB3 — Wrap final-boss battle and end-game reconciliation scene~~ — built, see F17. Not a combat battle in the end -- an ingredient-delivery ritual plus a jump-in to complete the ultimate crunch wrap, per the user's direct design. **The full 7-level MVP is now complete.**
- **FB4** — Full art pass across all characters/levels for visual consistency. **Assessed 2026-07-21, not built:** unlike every other art item fixed this session (L3/4/5/7 window-views, L6 variety, HeatZone texture -- each had a concrete, specific, visually-confirmed defect), this backlog item has no concrete target -- no list of what's actually inconsistent. Every generation prompt in `generate-sprites.ps1`, across the entire project history, reuses the same `$styleSuffix` ("Flat vector illustration style, thick clean outlines, bright saturated colors, flat shading, no gradients...") plus character reference images for anything with an established design -- visual consistency has been a structural property of the pipeline by construction, not left to chance. Blindly regenerating dozens of already-shipped, already-good assets on a vague "consistency" goal (with no way to verify the result is actually more consistent than what exists) isn't a good use of generation spend. If there's a specific asset that reads as inconsistent, that's a concrete, cheap fix (same pattern as this session's other art defects) -- worth naming directly rather than commissioning a full pass.
- ~~FB5 — Audio/music/SFX pass~~ — fully built: SFX (F36) and ambient loop (F37), both synthesized in-engine since no audio-gen API is configured. **Ambient loop's actual pleasantness is unverified by the AI (can't hear it) -- flagged in F37, worth a listen.**
- **FB6** — 2-player local co-op with Cheeto. **Investigated 2026-07-21** (the backlog item's own scope -- "investigation," not a build) rather than blind-implemented, because unlike everything else built this session, this one has no existing spec to build against: `characters.txt` lists Cheeto as "Concept only -- no visual design or mechanics required for MVP," meaning there's no silhouette, pose set, or personality to work from at all (every other character in this game got a full 8-pose grid before being wired into code). Findings:
  - **Architecture is single-player-specific today, not just single-player-configured:** `Player.tscn`'s `Camera2D` is a direct child of the player node with a hardcoded follow (see the FB29/FB30 camera work this session) -- a second player needs the camera decoupled to somewhere level-owned, tracking both players' midpoint with dynamic zoom to keep both on screen (simple shared-screen co-op, not split-screen, given the game's fixed 1280x720 viewport). `project.godot` has zero custom `[input]` actions -- everything runs on Godot's built-in `ui_*` defaults, so a second player needs a real second control scheme (e.g. WASD + a jump key) registered as new input actions.
  - **Enemy/hazard code should mostly just work:** `boss.gd` and every hazard script key off `is_in_group("player")` rather than a specific node reference, so a second `CharacterBody2D` in that group would likely be picked up by existing stomp/sleep/projectile/hazard logic with minimal changes.
  - **Real, unresolved game-design fork, not an engineering question:** does a hit cost from one shared life pool, or does each character have their own? Does Cheeto mirror Cornchip's full moveset (spin-dash, tomato seeds, climbing) or have a distinct kit? These change the actual feel of co-op and aren't answerable from the existing docs.
  - **Recommendation:** Cheeto's visual design (the same generation pipeline Cornchip/Wrap/every boss went through -- silhouette, personality, an 8-pose grid) is the real blocker, not the camera/input engineering, which is comparatively mechanical once a character exists to test it against. Building the 2P scaffolding before Cheeto exists risks re-testing all of it once real art lands anyway. Not built this session for that reason -- flagged here with concrete findings rather than either silently skipped or guessed at.
  - **Both blockers resolved 2026-07-21** (Cheeto's design confirmed as a cheese puff; shared lives + shared moveset confirmed) and the prototype built same-day -- see F38. Camera decoupling and input-action work landed as predicted, mechanically, once a character existed. **Not yet rolled into the real 7 levels or human-playtested** -- see F38's own limitations list.
- **FB7** — "Spin dash" upgrade: Cornchip spins rapidly, letting him plow through enemies unharmed while they comically fly off-screen (front- or back-flipping). Unlocked via the Air Fryer collectible (see below). Candidate for the ability-gated upgrade system (FB1).
- ~~FB8 — Stomp-to-defeat~~ and ~~FB9 — Per-level boss battles~~ — built, see F5. Cheese and Salsa Bowl are built as regular enemies (see F6); Avocado is now built too (see F12). Further stomp-triggered upgrades (beyond the baseline stomp) are still backlog.
- ~~FB10 — Air Fryer collectible~~ — built, see F8.
- ~~FB11 — Tomato collectible~~ — built, see F35.
- ~~FB12 — Lettuce as the main collectible token~~ — superseded: the collectible is beans, not lettuce (additions.txt correction), built as F7. Lettuce stays an ingredient-only concept.
- ~~FB13 — On-screen bean token counter, broadened to sprite + sparkle + high-score persistence~~ — built, see F31 (counter/sparkle/persistence) and `81eb5d7`/commit history (bean sprite generation).
- ~~FB14 — Cheese's "sleep for 10 seconds" effect~~ — built, see F6.
- ~~FB15 — Queso Grande and Jalapeño, Lime and Onion, Big Red and Cherry Tomato, Sour Cream Sam/Ice Cube/Chive Bit, Iron Skillet and Grease Splatter~~ — all built, see F10/F12/F13/F14/F15. Every boss/enemy through Level 6 now has art and code; only Wrap (Level 7's final boss) remains, and his art already exists from early Phase 1 work.
- ~~FB16 — World map redesign~~ — built, see F34.
- ~~FB17 — Ingredient delivery "shopping basket" UI~~ — built, see F31.
- ~~FB18 — Air Fryer onboarding~~ — the fly-to-HUD-icon half built, see F32. The first-time-paused-explainer half deliberately scoped out (not in the user's own raw feedback, and the no-reading-required pillar rules out a text version).
- ~~FB19 — Spin-dash cooldown~~ — built, see F30.
- ~~FB20 — Enemy AI free-roam rework~~ — built, see F30.
- ~~FB21 — Ending screen showing Cornchip having saved the day / made the wrap~~ — originally flagged as "let's discuss when we get to this," built 2026-07-21 once the user asked to proceed on everything without further check-ins. See F34.

## Explicitly Out of Scope (MVP)
- Touch/mobile input
- Text-based instructions or dialogue
- Multiplayer
